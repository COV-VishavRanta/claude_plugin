# Backend Implementation Plan: {Story Title}

> Generated by implementation-plan agent | Date: YYYY-MM-DD | Version: vX.X
> ADO Work Item: {ID} | Rev: {N} | State: {State} | Points: {Points}
> Parent Feature: {FEATURE-ID} — {Feature Title} | Feature Workflow: {path or "N/A — generated from story AC"}
> Frontend Plan: `{STORY-ID}-frontend-implementation-plan.md`
> ado-rev: {N}
> content-fingerprint-title: {first 50 chars of Title}
> content-fingerprint-description: {first 100 chars of Description}...{last 50 chars of Description}
> content-fingerprint-ac: {first 100 chars of Acceptance Criteria}...{last 50 chars of Acceptance Criteria}
> content-fingerprint-attachments: {count}:{sorted comma-separated attachment names, or "none"}

---

## Story Overview

{2-3 sentence summary from ADO description}

---

## New Library Needs

{Libraries added to `project-config.md` during this run with user approval, or "No new libraries required."}

---

## Frontend Data Requirements Summary

{Brief summary of what the frontend needs — derived from the frontend implementation plan. Ensures this backend plan is purpose-driven by UI needs.}

| Frontend Action / View | Data Required                             | Expected API Operation  |
| ---------------------- | ----------------------------------------- | ----------------------- |
| {action/view}          | {data description + pagination/filtering} | {operation name / path} |

---

## API Implementation

### Endpoints / Operations

| Method / Type             | Path / Operation         | Controller / Resolver | Description   | Auth Required | Confidence |
| ------------------------- | ------------------------ | --------------------- | ------------- | ------------- | ---------- |
| {GET/POST/Mutation/Query} | {path or operation name} | `{Class}.{method}()`  | {description} | Yes / No      | ✅/⚠️/❓   |

### Files & Structure

{All backend files to be created or modified. Full relative path from project root.}

| File Path                  | Action                | Key Contents                                                  | Confidence |
| -------------------------- | --------------------- | ------------------------------------------------------------- | ---------- |
| `{path/to/file.py or .ts}` | 🆕 CREATE / ✏️ UPDATE | {class names, function names, decorators — no implementation} | ✅/⚠️/❓   |

### Services & Functions

{Function name, parameters, return type, and responsibility — NOT the implementation body.}

| Service / Class | Function Name   | Parameters           | Return Type  | Responsibility               | Confidence |
| --------------- | --------------- | -------------------- | ------------ | ---------------------------- | ---------- |
| `{ClassName}`   | `method_name()` | `(param: Type, ...)` | `ReturnType` | {single-line responsibility} | ✅/⚠️/❓   |

### DTOs / Input-Output Types

| Name         | Type           | Fields               | Validation Rules                   | Confidence |
| ------------ | -------------- | -------------------- | ---------------------------------- | ---------- |
| `{TypeName}` | Input / Output | `{field: Type, ...}` | {required, min/max, pattern, etc.} | ✅/⚠️/❓   |

### Guards / Middleware

{Include only if the story adds or modifies guards, middleware, or interceptors. Omit if not applicable.}

| Name     | Type                                         | Purpose   | Applied To                    | Confidence |
| -------- | -------------------------------------------- | --------- | ----------------------------- | ---------- |
| `{Name}` | Guard / Middleware / Interceptor / Decorator | {purpose} | {route/resolver/module scope} | ✅/⚠️/❓   |

---

## Database Implementation

{If no database changes are required for this story, replace this entire section with: "No database changes required for this story."}

### Data Model Changes

{Describe new or modified entities and relationships at a conceptual level.}

```mermaid
{ER diagram of new/modified tables. Follow Mermaid Diagram Standards skill.}
```

### Migrations / Schema Changes

| Migration            | Table(s)  | Change Type                       | Description   | Confidence |
| -------------------- | --------- | --------------------------------- | ------------- | ---------- |
| `{timestamp}_{name}` | `{table}` | CREATE / ALTER / ADD INDEX / DROP | {description} | ✅/⚠️/❓   |

### Migration Outline

{Brief column definitions, constraints, and index specs for each migration above. Pseudocode or SQL — no ORM-specific syntax.}

### Seed / Test Data

{Include only if the story requires seed or fixture data. Omit if not applicable.}

| Table     | Records | Purpose                                  | Confidence |
| --------- | ------- | ---------------------------------------- | ---------- |
| `{table}` | {count} | Dev seed / Test fixture / Reference data | ✅/⚠️/❓   |

---

## API Contracts (Full Detail)

{Authoritative request/response contracts for every endpoint/operation defined above. The frontend plan references these.}

### {Operation Name} — `{METHOD} {path}` or `{mutation/query name}`

**Request:**

```json
{
  "field": "Type — description, constraints"
}
```

**Response (Success):**

```json
{
  "field": "Type — description"
}
```

**Response (Error):**

```json
{
  "error": "error_code",
  "message": "Human-readable description"
}
```

{Repeat for each additional endpoint/operation}

---

## Backend Task Checklist

### Phase 1 — Database Changes

{Omit this phase if no database changes are required.}

- [ ] 🆕/✏️ {Task — migration file name + table + change description}
- [ ] 🆕/✏️ {Task — e.g., "Create seed data for `webhook_credentials` dev environment"}

### Phase 2 — API / Service Implementation

{Ordered by dependency — DTOs and base types before services, services before resolvers/controllers.}

- [ ] 🆕/✏️ {Task — e.g., "Create `CreateWebhookCredentialInput` in `app/graphql/schemas/webhook_credential.py`"}
- [ ] 🆕/✏️ {Task — e.g., "Implement `WebhookCredentialService.create()` in `app/services/webhook_credential_service.py`"}
- [ ] 🆕/✏️ {Task — e.g., "Add `createWebhookCredential` mutation in `app/graphql/resolvers/webhook_credential.py`"}

---

## Dependencies / Blocking Items

| Dependency   | Type                                        | Status                                | Blocker? | Notes   |
| ------------ | ------------------------------------------- | ------------------------------------- | -------- | ------- |
| {dependency} | Story / Feature / Infrastructure / External | ✅ Done / ⚠️ In Progress / ❓ Unknown | Yes / No | {notes} |

---

## Open Questions / Risks

- {max 6 bullets — unresolved schema decisions, migration risks, auth scope questions, integration gaps}
